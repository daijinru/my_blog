<h1 class="articles articles__head">Web CAT</h1> 

<table class="articles">
    <tr class="articles article__thead">
        <th>Title</th>
        <th>Description</th>
        <th>Link</th>
    </tr>
    
    <% @articles.each do |article| %>
        <tr>
            <td><%= article.title %></td>
            <td><%= article.desc %></td>
            <td><%= link_to 'Show', article_path(article) %>
                <% if current_user %>
                    <%= link_to 'Edit', edit_article_path(article) %>
                    <%= link_to 'Delete', article_path(article),
                        method: :delete,
                        data: { confirm: 'Are you sure?' } %>
                <% end %>
            </td>
        </tr>
    <% end %>
</table>

<div><%= link_to 'New article', new_article_path %></div>
<div><%= link_to 'Home Page', welcome_index_path %></div>
